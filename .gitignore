# Keystone Part 7A — ship this on day one so `git add -A` is always safe and complete.
# A prior build's first deploy produced 0 pages because src/ and public/ were never pushed.
node_modules/
dist/
.astro/
.vercel/
.env
.env.*
!.env.example
.DS_Store
*.log
_typecheck/
